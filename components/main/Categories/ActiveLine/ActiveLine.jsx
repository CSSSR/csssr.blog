import React from 'react'
import styled from '@emotion/styled'

import styles from './ActiveLine.styles'

const ActiveLine = ({ className }) => {
  return <span className={className} />
}

export default styled(ActiveLine)`
  ${styles}
`
