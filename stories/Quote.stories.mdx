import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks'
import { compilerMdx } from '../components/Post/Body/utils/compilerMdx'
import {
  dataByLanguagePostAndImageDontResize,
  dataByLanguagePostAndImageResize,
} from '../lib/allPostsImages'
import { getStringAttributes } from './utils/getStringAttributes'
import { GridDecorator } from './decorators/GridDecorator'

<Meta
  title="Post/Quote"
  decorators={[GridDecorator]}
  argTypes={{
    type: {
      type: 'select',
      options: ['blue', 'black'],
    },
  }}
/>

export const Quote = ({ contentMdx, ...args }) => {
  return (
    <>
      {compilerMdx({
        content: `<Quote ${getStringAttributes(args)}>${contentMdx}</Quote>`,
        images: dataByLanguagePostAndImageDontResize['en']['offshore-web-development'],
      })}
    </>
  )
}

# [`<Quote />`](http://s.csssr.ru/U31J879TR/20200527125130.jpg)

Компонент цитаты. Имеет два вида, с изображением слева и с кавычками вместо изображения. Изображения для этого компонента лежат в папке `public/components/quote`. Принимает следующие пропсы

## `author`

Тип данных: `string`

Дефолтное значение: `undefined`

Имя автора цитаты.

## `position`

Тип данных: `string`

Дефолтное значение: `undefined`

Должность автора цитаты.

## `type`

Тип данных: `blue` | `black`

Дефолтное значение: `black`

Отвечает за внешний вид компонента цитаты без изображения. При `blue` цвет текста в цитате синий, а кавычки черные, при `black` наоборот.

## `linkText`

Тип данных: `string`

Дефолтное значение: `undefined`

Текст ссылки под цитатой.

## `linkHref`

Тип данных: `string`

Дефолтное значение: `undefined`

URL ссылки под цитатой.

## Результат

<Canvas>
  <Story
    name="Default"
    args={{
      type: 'black',
      author: 'Тим Бернерс-Ли',
      linkText: 'Записка о браузере WorldWideWeb',
      linkHref: '#',
      contentMdx: `Я написал программу на компьютере NeXT. Это дало мне преимущество в виде замечательных инструментов — классная была среда в целом. По сути, я мог за пару месяцев сделать то, что пришлось бы делать около года на других платформах, потому что на NeXT многое уже было сделано за меня. `,
    }}
  >
    {Quote.bind({})}
  </Story>
</Canvas>
