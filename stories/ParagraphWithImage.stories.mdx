import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';
import { compilerMdx } from '../components/Post/Body/utils/compilerMdx';
import { dataByLanguagePostAndImageDontResize } from '../lib/allPostsImages';
import { getStringAttributes } from './utils/getStringAttributes';
import { GridDecorator } from './decorators/GridDecorator';

<Meta
  title="Post/ParagraphWithImage"
  decorators={[GridDecorator]}
  argTypes={{
    imageName: {
      type: 'radio',
      options: ['manWithLaptop', 'laptopNews', 'laptopDialog'],
    },
    imageSide: {
      type: 'radio',
      options: ['left', 'right'],
    },
  }}
/>

export const ParagraphWithImage = ({ contentMdx, ...args }) => {
  return (
    <>
      {compilerMdx({
        content: `<ParagraphWithImage ${getStringAttributes(
          args
        )}>${contentMdx}</ParagraphWithImage>`,
        images: dataByLanguagePostAndImageDontResize['en']['example'],
      })}
    </>
  );
};

# [`<ParagraphWithImage />`](http://s.csssr.ru/U31J879TR/20200527123603.jpg)

Компонент параграфа c изображением в левом углу. Изображения для параграфа должны находится в той же папке, что и для компонента [`<Img />`](#img-). Принимает следующие пропсы

## `imageName`

Тип данных: `manWithLaptop` | `laptopNews` | `laptopDialog`

Дефолтное значение: `undefined`

Название изображения в папке, где лежит статика для этого компонента.

## `postImageName`

Тип данных: `string`

Дефолтное значение: `undefined`

Название изображения в папке, где лежат изображения для статьи.

## Результат

<Canvas>
  <Story
    height="250px"
    name="Default"
    args={{
      imageName: 'manWithLaptop',
      contentMdx: `##Venenatis cras sed felis eget velit. <p>Consectetur libero id faucibus nisl tincidunt.</p>`,
    }}
  >
    {ParagraphWithImage.bind({})}
  </Story>
</Canvas>
