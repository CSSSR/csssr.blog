import * as React from 'react'

import { storiesOf } from '@storybook/react'

storiesOf('AnimatedButton', module).add('AnimatedButton', () => {
  return (
    <div style={{ margin: 10 }}>
      <button>!!!!</button>
    </div>
  )
})
